rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Event logos: authenticated admins can write; anyone can read
    match /logos/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
